{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "dsl2doql/commands/query",
  "title": "DOQL command: QUERY",
  "description": "Adopted from wellmanifest.dev/schemas/dsl-commands/query",
  "x-adopts": "https://wellmanifest.dev/schemas/dsl-commands/query",
  "type": "object",
  "required": ["verb", "target"],
  "properties": {
    "verb": { "const": "QUERY" },
    "target": { "type": "string", "pattern": "^doql://" },
    "file": { "type": "string" },
    "format": { "enum": ["json", "yaml", "less"], "default": "json" }
  },
  "additionalProperties": false
}
