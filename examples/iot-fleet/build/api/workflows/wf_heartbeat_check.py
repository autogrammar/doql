"""Workflow implementation generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: schedule "*/1 * * * *"

def step_foreach_node(ctx: dict):
    '''Action: foreach node'''
    # TODO: implement foreach node
    pass


def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name='heartbeat_check',
        context=context,
        steps=[
    Step(
        name='foreach node',
        action=lambda ctx: step_foreach_node(ctx),
    ),
        ],
    )


# Register with engine
engine.register('heartbeat_check', create)
